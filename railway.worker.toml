[build]
dockerfilePath = "Dockerfile.worker"

[deploy]
startCommand = "cd /app/backend && python -m app.agent.interview_agent start"
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 5
healthcheckPath = ""
